---
# =============================================================================
# Istio — Scale (no-op)
# =============================================================================

- name: "istio | Scale — no-op"
  ansible.builtin.debug:
    msg: "istio scale: nothing to do"
